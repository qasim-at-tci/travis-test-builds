os: linux
dist: xenial
arch: Arm64
language: shell

install: skip
script: sleep 10

jobs:
  include:
    - dist: bionic
    - dist: focal
    - dist: jammy
deploy:
  provider: script
  on:
    all_branches: true
  script: echo "Hello"
