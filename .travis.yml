os: linux
dist: focal

services: postgresql
addons:
  postgresql: '12'
  apt:
    packages:
      - postgresql-12
      - postgresql-client-12
env:
  global:
    - PGPORT: 5433
install: skip
script:
  - psql -c "CREATE DATABASE test;" -d postgres
