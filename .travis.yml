os: linux
dist: bionic
language: python
python:
  - '3.7'
install:
  - pip install tox
jobs:
  include:
    - os: osx
      osx_image: xcode13.2
      language: objective-c
      install: skip
      script:
        - echo "DONE"
        - sleep 1
