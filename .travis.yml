---
language: node_js
os:
- linux
dist: focal
jobs:
  include:
  - stage: Test
    services:
    - xvfb
    addons:
      chrome: stable
    script: sleep 10
  - stage: Docker Image Build
    language: shell
    if: branch =~ /^(integration$|hotfix\/|release\/)/
    env:
    - NODE_ENV: production
    install:
    - skip
    script: sleep 15
