os: linux
dist: focal

language: ruby

#before_install:
#  - gem install bundler:2.2.33

install:
  - rvm uninstall 3.0.7
  - rvm install 3.0.7 --disable-binary
  - rvm use 3.0.7
  - gem install bundler:2.2.33
  - bundle install
script: sleep 10

jobs:
  include:
    #- ruby: 3.0.7
    #  dist: xenial
    #- ruby: 3.0.7
    #  dist: bionic
    - ruby: 3.0.7
      dist: focal
    #- ruby: 3.0.7
    #  dist: jammy
