language: shell

os: linux
install: skip
script: pwd
jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy

#deploy:
#  edge: true
#  provider: pages:git
#  cleanup: false
#  local_dir: storybook-static
#  keep_history: false
#  token: $GITHUB_TOKEN
#  on:
#    all_branches: true
