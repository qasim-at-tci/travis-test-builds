jobs:
  include:
  - stage: One
    os: linux
    dist: focal
    language: shell
    script:
      - echo done
      
  - stage: Two
    os: linux
    dist: focal
    language: shell
    script:
      - echo done
