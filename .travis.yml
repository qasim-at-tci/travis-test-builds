os: linux

language: go
go:
  - 1.18.x

install: skip
script:
  - cat /proc/cpuinfo
  - cat /proc/cpuinfo | grep avx512)
  - egrep -c '(vmx|svm)' /proc/cpuinfo
jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy
