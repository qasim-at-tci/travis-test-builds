dist: focal
language: generic

install: skip

script:
  - chmod +x ./hello.sh
  - ./hello.sh

jobs:
  include:
    - language: shell
    - language: generic
    - language: node_js
      node_js: lts/*
