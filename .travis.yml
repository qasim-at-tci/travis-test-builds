os: linux
dist: focal

language: shell
install: skip

script:
  - |
    set -e
    if [ "$CI" = "true" ]; then
      echo "Hello, World."
    fi
  - if [ "$CI" = "true" ]; then echo "Hello, World..." fi
