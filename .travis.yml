os: linux
dist: jammy

language: ruby
ruby: 3.2.4

before_install:
  - gem install bundler -v 2.5.10

install:
  - bundle install
script: sleep 10

jobs:
  include:
    #- dist: xenial
    #- dist: bionic
    - dist: focal
    #- dist: jammy
