jobs:
  include:
    - os: linux
      dist: xenial
      language: java
      jdk: openjdk9
      before_install:
      - java -version
      script: true
    - os: linux
      dist: trusty
      language: android
      jdk: openjdk11
      before_install:
      - java -version
      script: true
    - os: linux
      dist: xenial
      language: java
      jdk: openjdk13
      before_install:
      - java -version
      script: true
