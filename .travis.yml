addons:
  artifacts: true

install: skip

jobs:
  include:
    - os: osx
      osx_image: xcode13.1
      language: shell
      script: true
    - os: osx
      osx_image: xcode14.2
      language: shell
      script: true
    - os: linux
      dist: focal
      language: shell
      script: true
      
#deploy:
#  provider: script
#  script: ./deploy
#  on:
#    branch: minimal-build-check
