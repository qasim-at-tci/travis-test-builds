os: linux
language: php
jobs:
  include:
    - dist: jammy
      php:
        - '8.2'

      services:
        - mysql

      addons:
        apt:
          update: true
          packages:
            - libonig-dev
      install: skip
      before_script:
        - apt update
      script:
        - true
