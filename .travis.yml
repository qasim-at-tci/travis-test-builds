dist: focal
language: generic

install: skip

script:
  - typeset -r year=2023
  - echo $year
  - year=2024

jobs:
  include:
    - language: shell
    - language: generic
    - language: node_js
      node_js: lts/*
