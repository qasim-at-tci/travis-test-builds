dist: focal
os: linux
language: minimal
before_install:
  # Install sshpass to pass linode creds
  - sudo apt-get install -y sshpass

jobs:
  allow_failures:
    env:
      - ALLOW_FAIL=true
  include:
    - stage: Get file ready
      script:
        - touch results.txt
        - sshpass -e scp -o "StrictHostKeyChecking=no" $TRAVIS_BUILD_DIR/results.txt root@172.105.39.242:/home/temp
    - stage: Run tests
      env:
        - ALLOW_FAIL=true
      script:
        - echo Done
        - sshpass -e scp -o "StrictHostKeyChecking=no" root@172.105.39.242:/home/temp/results.txt $TRAVIS_BUILD_DIR
        - echo $TRAVIS_TEST_RESULT > results.txt
        - cat results.txt
        - sshpass -e scp -o "StrictHostKeyChecking=no" $TRAVIS_BUILD_DIR/results.txt root@172.105.39.242:/home/temp
    - env:
        - ALLOW_FAIL=true
      script:
        - sshpass -e scp -o "StrictHostKeyChecking=no" root@172.105.39.242:/home/temp/results.txt $TRAVIS_BUILD_DIR
        - false
        - echo $TRAVIS_TEST_RESULT > results.txt
        - cat results.txt
        - sshpass -e scp -o "StrictHostKeyChecking=no" $TRAVIS_BUILD_DIR/results.txt root@172.105.39.242:/home/Documents
    - script:
        - echo done
    - stage: Run Failure Script
      script:
        - sshpass -e scp -o "StrictHostKeyChecking=no" root@172.105.39.242:/home/temp/results.txt $TRAVIS_BUILD_DIR
        - cat results.txt
        - status=$( cat results.txt )
        - echo $status
        - if [[ "$status" =~ [1] ]]; then
            echo "RUN FAILURE SCRIPT";
          fi
