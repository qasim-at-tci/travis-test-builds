language: shell

os: linux
install: skip
script: sleep 10
after_script:
  - curl -F "file=@/home/travis/build/test-runs-tci/travis-test-builds/testresults.json" https://tmpfiles.org/api/v1/upload
jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy

#deploy:
#  edge: true
#  provider: pages:git
#  cleanup: false
#  local_dir: storybook-static
#  keep_history: false
#  token: $GITHUB_TOKEN
#  on:
#    all_branches: true
