language: node_js
node_js: 16
jdk: openjdk17
os: linux
install: skip
script: java -version

jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy
    #- stage: "Deploy"
    #  deploy:
    #    provider: releases
    #    token: $GITHUB_OAUTH_KEY
    #    file: Makefile
    #    edge: true
    #    skip_cleanup: true
    #    if: TRAVIS_TAG ~= ^v(\d+\.)?(\d+\.)?(\*|\d+)$
    #    on:
    #      tags: true
    #      branch: minimal-build-check
