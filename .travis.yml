os: linux
dist: jammy
language: shell
install: skip

jobs:
  include:
    - arch: arm64
      script: true
    - arch: amd64
      script: true

before_deploy:
  - rvm install 2.7.8 && rvm use 2.7.8 && gem install dpl
deploy:
  provider: releases
  token: $GITHUB_TOKEN
  edge: true
  on:
    branch: minimal-build-check
