language: java
os: linux
dist: xenial
install: skip

jobs:
  include:
    - dist: xenial
      script: mvn -v
    - dist: bionic
      script: mvn -v
    - dist: focal
      script: mvn -v
    - dist: jammy
      script: mvn -v
