language: shell

os: linux
install: skip
script: ruby -v
jobs:
  include:
    #- dist: xenial
    - dist: bionic
    - dist: focal
      group: previous
    #- dist: jammy

deploy:
  edge:
    branch: v2.0.3-beta.4
  provider: pages:git
  cleanup: false
  local_dir: storybook-static
  keep_history: false
  token: $GITHUB_TOKEN
  on:
    all_branches: true
