os: linux
dist: focal

language: shell
install: skip

script:
  - |
    set -e
    if [ "$CI" = "true" ]; then
      echo "Hello, World."
      sleep 10
      exit 1
    fi
  - if [ "$CI" = "true" ]; then echo "Hello, World..." fi
  - sleep 5
