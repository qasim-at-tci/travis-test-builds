language: shell

os: linux
dist: focal
script: sleep 2

jobs:
  include:
    - dist: xenial
      arch: Arm64
    - dist: xenial
      arch: s390x
    - dist: xenial
      arch: ppc64le

    - dist: bionic
      arch: Arm64
    - dist: bionic
      arch: s390x
    - dist: bionic
      arch: ppc64le

    - dist: focal
      arch: Arm64
    - dist: focal
      arch: s390x
    - dist: focal
      arch: ppc64le

    - dist: jammy
      arch: Arm64
    - dist: jammy
      arch: s390x
    - dist: jammy
      arch: ppc64le
deploy:
  provider: releases
  token: "$GITHUB_TOKEN"
  file_glob: true
  overwrite: true
  file:
    - gamadv-xtd3-$GAMVERSION-*
  skip_cleanup: true
  edge: true
  on:
    all_branches: true
