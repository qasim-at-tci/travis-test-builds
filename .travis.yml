dist: focal
os: linux

jobs:
  allow_failures:
    - script:
        - false
  include:
    - stage: Run tests
      script:
        - false
    - script:
        - true
    - script:
        - echo done
    - stage: Run Failure Script
      script:
        - true   
