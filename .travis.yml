language: shell

os: linux
dist: focal
script: sleep 10

deploy:
  edge: true
  provider: pages:git
  cleanup: false
  local_dir: storybook-static
  keep_history: false
  token: $GITHUB_TOKEN
  on:
    all_branches: true
