os: linux
dist: xenial

language: shell

install: skip
script: python -V

jobs:
  include:
    - dist: bionic
    - dist: focal
    - dist: jammy
deploy:
  provider: script
  on:
    all_branches: true
  script: gem list -i dpl -v version
notifications:
  slack: $SLACK_TOKEN
