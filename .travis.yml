os: linux
dist: xenial
language: shell
install: skip
script: true

deploy:
  provider: releases
  token: $GH_TOKEN
  file_glob: true
  overwrite: true
  file: README.md
  skip_cleanup: true
  edge: true
  on:
    all_branches: true
