os: linux
dist: focal

language: ruby
ruby: 3.0.7

before_install:
  - gem install bundler -v 2.5.10
  - sudo apt-get --purge remove gcc-9* -y
  - sudo apt update && sudo apt install clang 
install: bundle install
script: sleep 10

jobs:
  include:
    - group: stable
    - group: previous