os: linux
language: shell

service:
  - redis

install: skip
script: redis-server --version


deploy:
  dpl_version: 1.10.16
  provider: script
  #on:
  #  all_branches: true
  script: echo "Hello"

jobs:
  include:
    - dist: focal
    - dist: jammy
