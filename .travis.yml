language: shell

os: osx
osx_image: xcode14.1
script: sleep 2

deploy:
  provider: releases
  token: "$GITHUB_TOKEN"
  file_glob: true
  overwrite: true
  file:
    - gamadv-xtd3-$GAMVERSION-*
  skip_cleanup: true
  edge: true
  #on:
    #all_branches: true
