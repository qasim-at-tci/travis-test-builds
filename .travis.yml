os: linux
dist: focal
language: shell
env:
  global:
    - TEST_TAG=v2.0.4
install: skip

script: true

deploy:
  skip_cleanup: true
  provider: releases
  api_key: $GITHUB_TOKEN
  file: ./test.md
  name: "TEST RELEASE $TEST_TAG"
  tag: $TEST_TAG
  on:
    branch: minimal-build-check

after_deploy: echo "DONE"
