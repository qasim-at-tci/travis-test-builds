# uses cache?
jobs:
  include:
    - os: osx
      osx_image: xcode12.5
      before_cache:
        - brew cleanup
      cache:
        directories:
          - $HOME/Library/Caches/Homebrew
      language: c
      compiler: 
        - gcc
      script: true
      addons:
        homebrew:
          packages:
            - bison
            - flex
