os: linux
dist: focal
language: shell

install: skip
script: sleep 10

jobs:
  include:
    - if: branch=qasim-at-tci-draft
      env: TRAVIS_PULL_REQUEST_IS_DRAFT=true
    - if: branch=qasim-at-tci-draft
      env: TRAVIS_PULL_REQUEST_IS_DRAFT=false
