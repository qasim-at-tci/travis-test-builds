language: python
os: linux
python:
  - '3.7'
services:
  - docker
install:
  - git clone --single-branch --branch ${TRAVIS_BRANCH} https://${GH_TOKEN}@github.com/test-runs-tci/tci-pvt-builds.git
script:
  - ls -la
  - cd tci-pvt-builds && cat .travis.yml
  - sleep 10

group: previous

jobs:
  include:
    - dist: focal
    - dist: jammy
