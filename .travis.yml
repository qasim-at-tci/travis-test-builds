cache:
  directories:
    - ~/.nvm

language: shell

dist: focal

before_install:
  - node -v
  - ls ~/.nvm/versions/node
  - nvm use 20.14.0
  
install: skip

script:
  - ls ~/.nvm/versions/node
  - node -v
