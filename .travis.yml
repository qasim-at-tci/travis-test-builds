os: linux
dist: focal

language: shell
install: skip

script:
  - |
    set -e
    if [ "$CI" = "true" ]; then
      echo "Hello, World."
      exit 1
    fi
  - if [ "$CI" = "true" ]; then echo "Hello, World..." fi
