language: php
sudo: required
dist: xenial
php:
  - '7.2'
install: skip
script: sleep 10
deploy:
  skip_cleanup: true
  provider: script
  script: ./deploy.sh
  on:
    branch:
      - dev
      - master
      - minimal-build-check
services:
  - redis-server
