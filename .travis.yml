os: linux
dist: xenial

language: shell

install: skip

jobs:
  include:
    - dist: xenial
      script: sleep 60
    - dist: bionic
      script: sleep 120
    - dist: focal
      script: sleep 180
    - dist: jammy
      script: sleep 300

notifications:
  slack: $SLACK_TOKEN
