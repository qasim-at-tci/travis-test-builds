os: linux
dist: focal
language: shell

jobs:
  include:
    - if: tag IS present
      script:
        - echo "Tagged build"
    - if: branch=main AND tag IS blank
      script:
        - echo "Untagged branch build"