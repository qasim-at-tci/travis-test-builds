language: ruby
ruby: 3.1.6

os: linux

install: skip
script: ruby -v
jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy

deploy:
  provider: script
  script: ruby --version
  edge: true
