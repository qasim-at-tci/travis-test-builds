jobs:
  include:
  - stage: test
    script:
      - echo "--"
  - stage: test
    if: env(tier) = stack
    script:
      - |
        if [ $current == 0 ];then
            echo "---"
        fi
os: linux
dist: xenial
language: shell
