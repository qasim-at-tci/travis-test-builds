os: linux
language: shell

install: skip
script: false

before_deploy:
  - |
    if [ $TRAVIS_TEST_RESULT == 1 ]; then
      export BUILD=success
    fi
deploy:
  provider: script
  script: ./deploy.sh
  on:
    branch: minimal-build-check
    condition: $BUILD = success
