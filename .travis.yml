os: linux
dist: jammy
language: shell
install: skip

jobs:
  include:
    - arch: arm64
      script: true
      
deploy:
  provider: script
  script: ./deploy
  edge: true
  on:
    branch: minimal-build-check
