os: linux
dist: xenial
language: php
php: 7.4
before_install:
   - nvm install --lts
   - nvm use --lts
script:
  - php -version
  - node -v
  - npm -v

#before_deploy:
#  - export CHANGELOG=$(cat CHANGELOG-EXAMPLE.md)
#  - echo $CHANGELOG

deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file: README.md
  body: "Test releases."
  on:
    repo: qasim-test-org/travis-test-builds
    branch: main
    tags: true
  skip_cleanup: true
