os: linux
dist: jammy

language: ruby
ruby: 3.0.7

before_install:
  - gem install bundler
  - sudo apt update && sudo apt install clang -y

install:
  - bundle install
script: sleep 10

jobs:
  include:
    #- dist: xenial
    #- dist: bionic
    - dist: focal
    #- dist: jammy
