os: linux

language: go
go:
  - 1.18.x

install: skip
script:
  - cat /proc/cpuinfo
  - egrep -c '(vmx|svm)' /proc/cpuinfo
jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy
