jobs:
  include:
    - os: linux
      dist: xenial
      arch: arm64
      virt: lxd
      language: shell
      script: true
    - os: linux
      dist: focal
      arch: arm64
      virt: lxd
      language: shell
      script: true
