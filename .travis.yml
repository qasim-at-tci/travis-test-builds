---
language: node_js
os:
- linux
dist: focal
jobs:
  include:
  - name: "STABLE"
    group: stable
    services:
    - xvfb
    addons:
      chrome: stable
    install: skip
    script: google-chrome --version
  - name: "DEV"
    group: dev
    services:
    - xvfb
    addons:
      chrome: stable
    install: skip
    script: google-chrome --version
