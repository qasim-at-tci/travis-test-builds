os: linux
dist: focal
language: shell

install: skip
script: echo env(TRAVIS_PULL_REQUEST_IS_DRAFT)

#jobs:
#  include:
#    - if: env(TRAVIS_PULL_REQUEST_IS_DRAFT)="true"
#    - if: env(TRAVIS_PULL_REQUEST_IS_DRAFT)="false"
