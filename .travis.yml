os: linux
dist: jammy

install: skip
script: sleep 10

jobs:
  include:
    - language: shell
    - language: nodejs
      node_js: lts/*
    - language: ruby
      ruby: 3.1.2
    - language: python
      python: 3.12
deploy:
  # dpl_version: 1.10.16
  provider: script
  on:
    all_branches: true
  script: echo "Hello"
