language: scala
scala: 2.13.3
arch: 
   - amd64
   - ppc64le
   
dist: focal

script:
   #- sudo mkdir -p /etc/sbt/
   #- sudo touch /etc/sbt/jvmopts
   - sudo ls -al /etc/sbt/ || true
   - sudo cat /etc/sbt/jvmopts || true
   - sbt test
