jobs:
  include:
    - os: linux
      dist: bionic
      arch: arm64
      virt: lxd
      language: shell
      script: true
    - os: linux
      dist: focal
      arch: arm64
      virt: lxd
      language: shell
      script: true
