os: linux
dist: xenial

language: ruby
ruby: 3.2.5

install: skip
script: ruby -v

jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy

notifications:
  slack: $SLACK_TOKEN
