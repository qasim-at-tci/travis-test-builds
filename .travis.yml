language: python
python:
- 3.10.8
os: linux
dist: jammy
services:
- docker
git:
  submodules: false
cache:
  pip: true
install: skip

jobs:
  include:
    - script:
        - docker build -t test:0.1 .
        - docker images
    - script:
        - chmod +x ./build
        - ./build
        - docker images
