# uses cache?
jobs:
  include:
    - dist: focal
      language: ruby
      ruby: 2.7.6
      addons:
        apt:
          mariadb: '10.3'
      before_install: sudo apt install -f -y
      install: skip
      script: true
