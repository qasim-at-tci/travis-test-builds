language: php

os: linux
dist: focal
script: php --version

jobs:
  include:
    - php: 8.1.29
    - php: 8.1
#deploy:
#  edge: true
#  provider: pages:git
#  cleanup: false
#  local_dir: storybook-static
#  keep_history: false
#  token: $GITHUB_TOKEN
#  on:
#    all_branches: true
