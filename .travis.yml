os: linux

jobs:
  include:
    - dist: bionic
      language: bash
      before_install:
        - pyenv install 3.8.1
        - pyenv global 3.8.1
        - nvm install 14
        - nvm use 14
      install: skip
      script:
        - python --version
        - node --version
