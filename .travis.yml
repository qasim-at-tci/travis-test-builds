jobs:
  include:
    - os: linux
      dist: xenial
      language: java
      jdk:
        - openjdk11

      script:
        - java -version
        - nproc
        - free -m
        
    - os: linux
      dist: bionic
      language: java
      jdk:
        - openjdk11

      script:
        - java -version
        - nproc
        - free -m
        
    - os: linux
      dist: focal
      language: java
      jdk:
        - openjdk11

      script:
        - java -version
        - nproc
        - free -m
