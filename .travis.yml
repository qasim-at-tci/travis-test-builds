language: php
php: 8.2
os: linux

install: skip
script: true

jobs:
  include:
    - stage: workaround
      dist: jammy
      addons:
        apt:
          update: true
          packages:
          - libonig-dev
    - stage: workaround
      dist: focal
      addons:
        apt:
          update: true
          packages:
          - libonig-dev
    - stage: workaround
      dist: bionic
      addons:
        apt:
          update: true
          packages:
          - libonig-dev
    - stage: workaround
      dist: xenial
      addons:
        apt:
          update: true
          packages:
          - libonig-dev
    - stage: without-workaround
      dist: jammy
      
    - stage: without-workaround
      dist: focal
      
    - stage: without-workaround
      dist: bionic
      
    - stage: without-workaround
      dist: xenial
      
