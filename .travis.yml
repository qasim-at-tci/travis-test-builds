os: linux
language: shell

addons:
    apt:
      update: true
      packages:
        - libu2f-udev
        - libvulkan1
    chrome: stable
    
install: skip
script: true

jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
